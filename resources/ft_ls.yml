programs:
  ls_regular:
    cmd: "ls /tmp"
    numprocs: 3
    workingdir: /tmp
    autorestart: never
    stdout: ./result/ls_reg.out.log
    stderr: ./result/ls_reg.err.log

  ls_root:
    cmd: "ls -R /"
    workingdir: /
    numprocs: 2